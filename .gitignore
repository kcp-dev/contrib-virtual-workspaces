/bin/
*.test
.kcp/
.local/
pki/

# Binaries built into the repo root by `go build ./cmd/...`. They shadow bin/ on
# tab-completion and go stale silently; use `make build`, which writes to bin/.
/ephemeral-virtual-workspace
/endpointslice-controller
/example-webhook
